// 「お気に入り登録」後の処理 => 「お気に入り解除」表示に変更
// liタグ中のグリフアイコンの追加
spanTag = $(".favorites__create").closest('li').find("span").clone(true)
// お気に入り数の抽出
var liTagInfo = /(\d+)/.exec( $(".favorites__create").closest('li').text() );
// 1) liタグの中身を空にする
// 2) グリフアイコンの追加
// 3) お気に入り登録ボタンの追加
// 4) お気に入り数のカウントアップ
$(".favorites__create").closest('li').text('').html( spanTag ).append( " <%= escape_javascript( render 'favorites/destroy', pattern_id: @pattern_id ) %>" ).append( `: ${ Number( liTagInfo[1] ) + 1 }` );
